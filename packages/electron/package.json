{
  "name": "@opencode-ai/electron",
  "version": "1.0.0",
  "description": "Electron wrapper for OpenCode",
  "author": "shashank",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "bun run build && electron .",
    "build": "bun build ./src/main.ts --target node --outfile ./dist/main.js --format esm --external electron && bun build ./src/preload.ts --target node --outfile ./dist/preload.cjs --format cjs --external electron",
    "pack": "bun run build && electron-builder --dir",
    "dist": "bun run build && electron-builder"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3",
    "typescript": "^5.9.3"
  }
}
